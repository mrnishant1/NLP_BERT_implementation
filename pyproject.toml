[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "youtube_extension"
version = "0.1.0"
authors = [{ name = "Nishant", email = "nishantkumaragra@gmail.com" }]
description = "youtube comment analysis"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
